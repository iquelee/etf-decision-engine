name: test

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        # P0-C：Node 16 对齐 CloudBase 生产 runtime（Nodejs16.13），Node 22 面向未来兼容
        node-version: ['16', '22']
        python-version: ['3.11', '3.12']
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}

      - name: Setup Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install Python deps (numpy/pandas)
        run: pip install numpy pandas

      # 门禁（P0-B：npm test 现已含 Stage F = build-cloudfunctions.js 的 Build Common Parity）：
      #   Stage A  Node 单元测试
      #   Stage B  Python 单元测试
      #   Stage C  Immutable 真 SHA 锁 + Gen-1 Feature Pipeline Lock（P0-A / G1-B）
      #   Stage D  Cross-language Parity（Gen-2）
      #   Stage E  Secret scan
      #   Stage F  Build Common Parity（src/common → 11 云函数 + SHA 校验）
      #   Stage G  Gen-1 Production Gates G1-A~H（Frozen/Pipeline/Parity/Permission/Domain/Circuit/Execution/No-op）
      - name: Run full test gates (npm test)
        env:
          TCB_PYTHON: python
        run: node scripts/test-all.js

      # Gate 6（显式，与 Stage C 同源）：Immutable 真 SHA 锁，任一 frozen 对象被改 → FAIL
      - name: Gate 6 — Immutable SHA (Gen-1 / V3.6.1 / GEN2 bundle)
        run: node scripts/verify-immutable.js

      # WP-G1 显式命名门禁：G1-A ~ G1-H（Frozen / Pipeline / Golden Parity / Permission / Domain / Circuit / Execution / No-op）
      - name: Gate G1-A~H — Gen-1 Production Gates
        run: node scripts/gen1-production-gates.js
