# Gen-2 报告索引（角色语义与账本口径）

> 规则实现于 2026-09-11 修正（WP-G2-03）后，**角色语义已唯一化**；回测账本于 2026-09-11
> 统一（WP-G2-02 / B1）。因此此前的经济类报告只能作为**审计基线**，不能与新结果逐位比较。

## 当前有效（唯一权威口径）

| 报告 | 内容 |
|---|---|
| `gen2_b1_ledger_baseline_20260911.md` | **B1 回测账本基线**：唯一权威角色语义 + 唯一权威账本，四类策略同口径比较 + 资金守恒验收 |
| `gen2_b1_research_baselines_20260911.md` | **B1 研究基线**：角色基线 / 归因 / 敏感性 + 组合有效性守卫。**F1（Alpha 显式注入 `selection_scores`）与 F2（显式 `role_thresholds`）已修**，替代 Alpha 场景现已真正影响组合；F3 费用档白跑已修（数值不变）；**F4 = WP-G2-06**（研究脚本组合构建丢弃权威权重上限、priority 用 legacy rank、缺防守腿 → 已升为资格阻断项，**2026-09-14 已开工**：红基线 + 实施计划见 `gen2_wp_g2_06_plan_20260914.md`）。生成器：`python -m gen2.baseline.rebuild_research_baselines`（检测到未登记/已过期的无效场景会直接报错） |
| `gen2_b1_research_rule_rotation_20260911.md` | 角色基线回测报告（新口径，Economic Gate = FAIL/UNPROVEN） |
| `gen2_rule_impl_correction_20260911.md` | 规则实现语义变更记录（D-001/D-002/D-004 收口、V2 角色权威唯一化、G2S-06 解锁） |
| `gen2_scenario_parity_20260911.md` | 双端场景比对报告（G2S-01..07 不变量与差异分类） |
| `gen2_pr26_rule_bundle_gate_20260914.md` | **PR #26 修订（WP-G2-05R）**：撤除 JS 运行时 `role_thresholds` fallback → 规则 bundle 闸门（`blocked` / `RULE_BUNDLE_INCOMPLETE`）+ G2S-09 双端 blocked 回归。合并后 Gen-2 Shadow 持续 blocked 直到 WP-G2-04 补齐 bundle —— **该中间态已于 2026-09-14 由 WP-G2-04 解除**（见 `gen2_wp_g2_04_freeze_20260914.md`；G2S-09 的 `bundle_selection_base` 同时改标为**合成基线**，不再声称镜像冻结 bundle）。**追加**：G2S-09 新增「数据源一读就抛错」陷阱 case（主控 + 正控），把「结果优先」升级为「**顺序**证明」（闸门位于所有数据读取之前） |
| `gen2_wp_g2_06_plan_20260914.md` | **WP-G2-06（F4）实施计划**：候选组合构建统一（保留权威单只/cluster/科技上限、`priority` 来自显式注入 score、补现金腿与防守腿）。四+三项验收边界与文件级步骤。**红基线测试不进主干**：故意为红的 pinning test 只存在于工作分支 `feat/gen2-wp-g2-06-tree`，实现全绿后才移入 `ml/gen2/tests/` 随实现一并提 PR |
| `gen2_wp_g2_06_impl_20260914.md` | **WP-G2-06（F4）实现报告**：统一候选组合构建（B1 权威权重沿用 / B2 注入 priority / B3 现金+防守腿 / B4 同口径 / B5 priority 完备性+hash / B6 账本 sleeve 归属 / B7 G2S-10 跨端逐日对表）。测试入正式目录 29/29；跨端 G2S-10 **0 差异** |
| `gen2_rule_impl_correction_20260914.md` | **规则实现修正记录（WP-G2-06 裁决回合）**：F4 **接入线上决策链**（`main()` 改走唯一 `buildCandidatePanel()`；旧 `buildPortfolioCandidates()`/`applyDefense()` 在 `main()` 中已无消费者）+ 三类记录发布契约（`gen2_ranking` 只承载 30 ETF；**新增 `gen2_candidate_leg`** = 30 ETF + CASH）+ **五处缺口修复**（①广义科技合计实际检查 ②防守后 PRE/POST_DEFENSE 双阶段复核 ③priority 严格集合校验先于 hash ④Python `defense_config` 透传 ⑤benchmark 字段名适配 `toBenchmarkRows`）。新增端到端回归 `tests/gen2-candidate-leg-e2e.test.js`（真实 `main()` 注入 DB，三向一致）。**语义变更 → WP-G2-04 重新冻结 bundle/lock（已完成）** |
| `gen2_b1_frozen_run_20260914.md` | **B1 冻结运行（Frozen Run）**：以 `gen2-rule-v2.0.1` 冻结锁为**唯一输入**重算 B1。运行前置门 = ①`immutable_set`（`lock_revision 3` 起 **8 项**）+ `build_artifacts` 2 项 SHA 逐位核验 ②运行配置 ↔ 冻结规则**零漂移**（17 个规则键）③跨实现常量交叉核对（regime 55/45 = `portfolio/regime.py`、alpha = canonical 等权）。隔离 run ID `b1_ledger_baseline_20260914_frozen_v201`（旧 `…20260911` 降为**审计基线**），完整 manifest：`ml/gen2/manifests/GEN2_B1_FROZEN_RUN_MANIFEST_20260914.json`。窗口 2020-03-10 → 2026-09-04（1577 日），资金守恒误差 0、现金非负 → **门禁 PASS**。**manifest 按裁决单列并校验四类哈希**：①最终 LOCK SHA + 8 项完整组件哈希（+ 折叠摘要）②运行侧实现哈希（`b1_frozen_run.py` / `rebuild_baselines.py` / `ledger.py` / `costs.py`）③输入行情 + ETF 主数据内容哈希、日期范围、环境版本 ④输出报告与指标文件哈希（`self_check` 逐项重算，失配即拒绝运行）。报告含 **§9 差异归因页**（旧审计基线 vs 新冻结基线：D-001 / F1-F2 / F4 / 统一账本各自改变了什么）。生成器：`python -m gen2.baseline.b1_frozen_run`。**经济资格仍未解除**：B3 Frozen OOS 通过前一切净值只作研发证据。**追加（第三次复核裁决）**：①产出**降级为「待合并冻结运行」**（`run_status = PENDING_MERGE`，标题带「· 待合并」）—— 不得先写成项目最终 Frozen B1，也不得据此启动 B3；②**方向性表述更正**：旧实现把现金腿重复计入换手 ⇒ 费用被**高估** ⇒ 旧净收益**被低估、表现更悲观**（此前写成「旧读数偏乐观」是错的）；③§9 增 **§9.0 可对照性判定 + §9.1 可审计数值对照表**（旧 vs 新 × 0/5/10/20bps × 逐策略 × 净值/CAGR/Sharpe/换手/成本/Δ），表格由**两边 `ledger_summary` 数据**生成 —— 旧基线读数固化为**入库快照** `ml/gen2/manifests/GEN2_B1_AUDIT_BASELINE_20260911.json`（20 行 = 5 策略 × 4 成本档）；§9.3 用**时点判定规则**（变更是否落在旧基线产出 14:21 **之内**）逐项标注，**在 2026-09-11 审计基线与本次相同窗口/数据的对照中，F4 是唯一实质来源**（D-001 / 统一账本已在旧基线内；F1/F2 默认等价；该口径**不**推广为对全部历史结果的普遍因果结论），并由 `F4MechanismValidityTest` 用**上限越界反例**证明机制有效；**窗口不一致时 Δ 一律显示 `n/a`**（防止拿两个不同问题相减、假报「基准 Δ ≠ 0 ⇒ 账本变了」）；④§12 增**合并接受条件** + `accept_merge(master_commit)`：写入前**重算三类摘要**（锁 SHA + 组件折叠摘要 / 输入内容摘要 / 运行实现摘要），任一变化即**拒绝写入并要求重跑**；三者一致才写 `accepted_master_commit` / `source_tree_sha`（落实「内容哈希不变则无需重跑」），措辞转「**Frozen B1 已接受**」后才允许启动 B3。**追加（第四次复核裁决）**：①**归因口径限定** —— 结论只在「2026-09-11 审计基线 vs 本次运行、相同窗口/数据」这组对照内成立，**不**扩展为普遍因果结论；②**新增流程闸门**：`--accept-merge` 会修改**受版本控制**的 manifest / 报告 ⇒ 它自身必须落在**独立的接受记录 PR**（分支约定 `chore/gen2-b1-accept-v201`）上 —— 顺序为「合并冻结 PR → 合并 B1 PR → 从新 master 建该分支并运行 → 开并合并该接受记录 PR」，此前措辞仍不得写「已接受」、不得启动 B3；③接受命令**额外三项 fail-closed 校验**：`<SHA>` 是 `origin/master` 的**祖先**、`<SHA>^{tree}` 与 manifest 记录的 `source_tree_sha`（**取证运行时**的 `HEAD^{tree}`）一致〔不等时**差异只允许**为本次运行自身产物 manifest/报告 —— 与「报告不含自身哈希」同构的自引用〕、当前**组件/输入/输出摘要**（含报告自身哈希）与 manifest 逐位一致；④运行侧新增 `source_state`（`git_head_commit` / `source_tree_sha` / `working_tree_dirty`）—— **先提交代码再跑 B1**，否则取证树不干净会被拒绝。**追加（第五次复核裁决 · 契约修正）**：① **`--accept-merge` 校验契约修正** —— 早期版本要求「`<B1 merge SHA>^{tree}` 严格等于 manifest 的 `source_tree_sha`」（后放宽为「差异只允许是本次运行产物」），方向是错的：报告 / manifest / 审计快照是**执行之后**才提交进 B1 分支的 ⇒ 两个 tree **必然不同**，要求相等**必然失败**。修正后的校验为 ⓪ **工作树干净**（接受时 + 取证时**两态**）→ ① **执行提交** `source_state.git_head_commit` 是 `<SHA>` 的**祖先**（方向是「执行提交 → `<SHA>`」，不是「`<SHA>` → `origin/master`」）→ ② **锁定组件 / B1 工具链 / 输入摘要 / 输出摘要**与 manifest **逐项**一致（逐项才能指出**哪一条锁定路径**变了，不只比折叠摘要）→ ③ `<SHA>` 的**树中包含**已承诺的**报告 / manifest / 审计快照**且**哈希匹配**（新增 `_git_blob_sha256`：取 `git cat-file blob <SHA>:<path>` 的**原始字节**再 CRLF→LF 归一化，与磁盘侧同口径 ——「本地有这份报告」证明不了「合并进去的是这份报告」）。记录改用 `accepted_master_commit` / `accepted_master_tree_sha`（**接受时仓库快照**）/ `execution_source_tree_sha`（**执行时语义源码快照**），并显式写入 `trees_required_to_be_equal=false` —— **不再要求两者相等**，二者的关联由 ② 的摘要族与 ③ 的已承诺证据哈希建立；`master_ref` 降为**信息性**字段（`master_ref_is_blocking=false`，本地无该引用时记 `None` 且**不阻断**接受）。② **新增并发中止码 `ABORTED_SOURCE_MUTATED`**：`run()` 结束前用 `source_mutation_report()` 重放「`HEAD` 是否移动 + `run_implementation` 逐项哈希是否变化」（**不能**看工作树脏否 —— `run()` 自己会写受控的 manifest / 报告，拿脏树判定会**恒真**），命中即写入该状态并 fail-closed 作废产出。它是**正确的 fail-closed 结果**（**不称「假失败」**），干净源上的重跑才是可采信的门禁证据。测试 50 → **61**（新增 `SourceMutationTest` 4 项、`GitBlobEvidenceTest` 3 项、`AcceptanceRecordTest` 13 项含「产物导致整树差异必须允许」与「锁定路径 / 工具链 / 证据缺失 / 证据哈希不匹配必须拒绝」四类反例）。**追加（CI 红复现修正 · 2026-09-15）**：PR #31 的 4 个 `test` job 全红，根因**不在被测实现**而在**测试自身** —— `RunInputHashTest` 的「无本地日线池（CI）」分支把 `input_data.content_digest` 写成魔数 `"0" * 64`，而 `verify_manifest` 的 `input.content_digest` 是由**声明的** `files` / `meta_files` 重算的（`_aggregate_digest([])`，**不读磁盘**）⇒ 任何没有日线池的环境**恒判不一致**；本地因有池走真 `input_data_attestation` 分支而看不出问题，属**「只有 CI 才红」的盲区**。修法：① 空输入声明改用 `_aggregate_digest([])` 自洽；② **新增 `test_empty_input_declaration_is_self_consistent`**，把该分支在**本地**显式跑一遍（等价于在 CI 上跑，不再依赖池）；③ `GitBlobEvidenceTest` 补「当前是 git 工作区」前提守卫 —— `git archive` 源码导出包上**显式跳过**，而不是制造一条与实现无关的失败。测试 **61 → 62**。复现口径：`git worktree add --detach <tmp> 3330e9c` 后 `PYTHONPATH=ml python -m unittest discover ml/gen2/tests`（真实 git、无未入库日线池）⇒ 修复前 `Ran 265 tests … FAILED (failures=1, skipped=17)`、修复后 `Ran 266 tests … OK (skipped=17)`；该树上 `node scripts/test-all.js` 由 **44/45 → 45/45**（失败项仅为 Stage B 的 `unittest discover`）。 |
| `gen2_b3_frozen_oos_plan_20260915.md` | **B3 协议（判据先于结果冻结）**：目标与不做 / 冻结对象（含「**B3 不使 B1 失效**」的边界声明）/ OOS 边界（沿用既有 walk-forward 口径，**不自定**）/ 账本连续只切片 / 成本口径 / 输出项 / 指标口径复用来源 / **H1–H7 硬门 + C1–C4 判据 + 否决规则**（复用 `PLAN_GEN2_1.md` §10 已定死的 G2/G7/否决规则，**不自创阈值**）/ 失败处置 / 产物清单 / 复现命令。其 SHA 进 manifest `protocol.sha256` |
| `gen2_b3_frozen_oos_20260915.md` | **B3 Frozen OOS（冻结规则样本外验证）**：判据**结果前**定死（协议见上）。OOS = 既有 `WalkForwardConfig(train_years=3, purge_days=20, embargo_days=5)` 各 fold test 段并集 = **2021-01-04 → 2026-09-04（1376 日）**，边界**未自定**；账本以与 B1 **同一入口**（`build_unified_baselines`）+ 同 calendar + 同 T+1 + 同期初全现金跑**全窗口**后**只切片** ⇒ 持仓与状态机在 OOS 起点连续、不冷启动。**9 道硬门全 PASS**：H1–H3 前置三门 + H4 输入内容摘要 == 已接受 B1 + H5a 锁定组件摘要一致 + H5b 与 B1 **同文件**的工具链条目哈希逐项一致（`baseline_builder`/`ledger`/`cost_impl`）+ **H5c 重算全窗口读数 == 已接受 B1 报告表逐位（140 单元格 / 0 失配）** + H6 OOS 段资金守恒（误差 0、现金非负、零超配）+ H7 执行窗口内源未被改动（`working_tree_dirty=false`、HEAD `30943b2`）。**结论 `FAIL`**：C1 累计净收益 @10bps **+37.54% vs Main5 +247.15%**、C2 Sharpe **0.43 vs 0.99**、C3 增量 bootstrap CI 下界 **-0.001477 < -1e-4**、C4 最差年份差 **-49.30% < -15pct**，C1–C4 全 FAIL；**否决规则生效**（OOS Rank IC = **+0.0271** 为正，但成本后仍输 Main5 ⇒ 按 §10 一律 FAIL，IC 不是资格）。**关键机制读数**：即便 **0 bps** 也输（**+57.16% vs +251.18%**）⇒ **费用不是根因**（换手 133.4 vs 11.5 只是放大器），根因在**多头年份的选池 / 仓位机会成本**（2023 -49.30pct、2025 -43.89pct 两个灾难年）；唯一亮点是 MDD **-23.35% vs -34.57%**（+11.22pct）与 2022 逆市年 **+24.43pct** ⇒ **防守腿有效、进攻端失效**。manifest：`ml/gen2/manifests/GEN2_B3_FROZEN_OOS_MANIFEST_20260915.json`（`self_check` **56/56**、`self_check_pre_report` 55/55）。生成器：`python -m gen2.baseline.b3_frozen_oos`。**处置**：维持 Shadow / CANARY，另立新假设研究；**禁止**据本次结果回调参数后重跑。**接受记录（PR #33 合并后收口）**：B3 PR #33 的合并提交为 `f8ef523c…`；随后由 `python -m gen2.baseline.b3_accept_record --accept-merge --master-commit <PR #33 merge SHA>` 写入接受记录 —— 四校验（⓪ 工作树干净（接受 + 取证两态）/ ① 执行提交 `30943b2f…` 是 `f8ef523c…` 的**祖先** / ② 锁定组件 · **B3 工具链 7 项** · 输入 · 输出摘要**逐项**一致 / ③ `f8ef523c^{tree}` 的树中含报告 · manifest · **协议**且 blob 哈希匹配）全过后，`run_status` 由 `PENDING_REVIEW` 绑定为 **`ACCEPTED_FAIL`**（`accepted_master_tree_sha = e8db236b…`、`trees_required_to_be_equal = false`、`no_rerun_required = true`）。⚠️ **接受的是结论与证据，不是「通过」**：`verdict = FAIL` 与 `economic_eligibility = NOT_PRODUCTION_ELIGIBLE` 均不变。报告措辞随之改为「失败取证已接受」，且**只改措辞、不动正文**（`assert_report_reproduces` 断言「从 manifest 复原数据重渲染**逐字**等于被承诺的那份」；接受**前**报告哈希 `e9050e18…` 记入 `bound_digests.committed_report_sha256`，接受**后**为 `e679e13a…`）。接受逻辑刻意放在**独立模块**（`ml/gen2/baseline/b3_accept_record.py`）：`b3_frozen_oos.py` **自身**是 `run_implementation` 的记录条目，改一字节即令校验 ② 失败 ⇒ 不能给它加子命令。**本运行自此正式归档为 Rule V2.0.1 的「已接受经济失败基线」。** |
| `gen2_wp_g2_04_freeze_20260914.md` | **WP-G2-04 冻结报告**：**只做冻结、不部署**。`bundle_version = gen2-rule-v2.0.1`；`selection.role_thresholds` 由旧字段离线换算烘焙（**语义等价，不是调参**，已用「参数未变」断言逐值钉住）；旧字段移入 `legacy_migration_audit`。lock 扩为 **bundle SHA + JS 实现 + Python 规则/候选/防守实现 + 构建产物哈希**（`verify-immutable.js` 8 → 19 项 + `ROOT_ANCHORS` 自锚同步）；新增 `scripts/verify-gen2-build-artifacts.js`（Stage F）证明**云函数实际加载的 bundle 与冻结源逐位一致**。新回归 `tests/gen2-rule-freeze.test.js` 32/0 + `test_wp_g2_04_freeze.py` 16/16。**规则完整性阻塞解除**（真实冻结文件驱动 `main()` → `completed`），但**经济资格未解除**。**追加（`lock_revision 2`，同日审查裁决）**：`immutable_set` 5 → **6 项**，新增 `python_role_thresholds`（`ml/gen2/portfolio/role_thresholds.py`）—— 它定义线上阈值**从哪读、读到什么算合法**，不锁定即存在「**bundle 字节未变、运行语义已变**」的旁路；扩围**不改 bundle 字节与版本**，只改 lock（`ROOT_ANCHORS` 同步为 `289cdec1…`），留痕于 `lock.lock_amendments`；`verify-immutable.js` 19 → **21 项**（新增 id 必需集合守卫）。**追加（`lock_revision 3`，同日第二次审查裁决）**：`immutable_set` 6 → **8 项**，新增 `python_selection_scores`（`ml/gen2/baseline/selection_scores.py`：显式 Alpha 的**计算 / 覆盖校验 / 内容哈希** —— 只比对权重常量锁不住运行语义）与 `python_regime`（`ml/gen2/portfolio/regime.py`：`RISK_ON_GE=55` / `RISK_OFF_LE=45` 是**实际执行常量**，bundle 里的 `regime` 只是声明，属同类旁路）；同样**不改 bundle 字节与版本**，`ROOT_ANCHORS` 同步为 `d3d40f99…`；`verify-immutable.js` 21 → **23 项**，冻结回归 `tests/gen2-rule-freeze.test.js` 32 → **37**、`test_wp_g2_04_freeze.py` 16 → **18**（含对两文件的**反「锁 A 读 B」**断言） |
| `gen2_attack_side_taskbook_20260915.md` | **Gen-2 进攻端失效归因与新假设设计 —— 研究任务书（M0 rev5，`APPROVED`）**：由用户 **2026-09-15 裁决**授权入库。核心是 **§2 三层证据模型**（Stage **M** POSTMORTEM / **R** RESEARCH / **Q** QUALIFICATION）与**禁止流动表** —— **M 层（已被观察段）只能产出机制假设、不得定阈值；R 层（O2 研究宇宙）只能证明「机制成立」；Q 层（O1 前瞻，当前 31 只池的未来数据）是唯一生产资格证据**。**§3.2 关键校正**：`universe_v1.json` 的 `data_note` 记载 10 只老 ETF（医药/酒/消费/红利/银行/券商/有色/煤炭/纳指/标普）「仅 640 根约 2.5 年、长历史待东财源补」，实测这 10 只**全部 640 行、起点同为 `2024-01-15`** ⇒ 这是**数据源截断而非上市日限制**，故 **O2 首选取数对象即这 10 只**（早已上市、可交易、其早期段从未进入任何 Gen-2 运行）。**§3.3 禁用清单**：B3 OOS `2021-01-04 → 2026-09-04`、**`2020-03-10 → 2020-12-31`（205 日）**、`2018-04-03 → 2020-03-06`（≈480 日、池仅 2–3 只）**三段均曾落在 walk-forward 训练窗内** ⇒ **禁止包装为新的 Frozen OOS**（「**未用于判定 ≠ 未被观察**」）。**§4** O2 取数方案（更早上市可交易 ETF 优先 / **指数回溯仅作机制参考、不得作资格证据** / O3 仅作跨资产机制否证 / **O4 取消**，理由是其 Development 只能落在已被观察的 `2020` 段）。**§5** M2 放行但限定为 **`POSTMORTEM_NOT_FOR_CALIBRATION`**（四层只读归因：选池 / 权重·上限 / 防守 / 换手·成本 + 五条禁令（禁参数搜索·网格·阈值调整·新 bundle/lock·为找通过结果重跑 OOS）+ 输出只能形成假设与任务书，**不得改变 `ACCEPTED_FAIL` 归档状态**）。**§6** 里程碑 M0–M7（**M7 = O1 前瞻资格**；**M6 前的任何结果都不得称为「生产资格」**）。**当前结论不变**：Rule V2.0.1 不具备生产资格，继续 Shadow / CANARY，不部署、不提权、不写正式仓位。**追加（M0 rev5，同日裁决）**：`PENDING_RULING` 悬空引用已闭合（§4.1 暖机期 → freeze §2.4 = **250 交易日**）；**O2 的 10 只老 ETF 必须以单一新来源（东财全历史前复权等）完整重取，禁止与现有腾讯序列直接拼接**；合并后**只可进入 M1 审计与取数准备**，仍不得进入新规则实现。 |
| `gen2_data_boundary_freeze_20260915.md` | **数据边界与访问纪律 —— M1 前置冻结（五项，`FROZEN v1.1`）**：**入库即生效**，后续工作包不得就地修改（变更须新 revision + 用户二元批准）。①**§6 数据边界审计模板**：逐只留档（真实上市日 / 场内可交易 / 复权源与**基准日** / 拼接锚点与校准因子 / 最长连续停牌 / 日均成交额 / **与已被观察段的重叠必须为空**）+ §6.2 生存偏差清单 + §6.3 段落汇总（结论 `PASS`/`CONDITIONAL`/`FAIL`）。②**§2 资产纳入规则**：M1–M7 必要条件 + **X1–X6 排除**（含 X5 已被观察段须剔除、X6 不得只纳入存续标的）+ **§2.5 生存偏差专项**（须列出区间内已清盘/合并/转型标的；无法取得则声明「存在未量化生存偏差」且结论只能作机制否证）+ **§2.4 `PENDING_RULING` 数值门槛表**（暖机期 / 停牌上限 / 流动性下限 / 最小标的数 / sector 覆盖 —— 本项目纪律**不自创阈值**，候选区间仅供裁决，须在 **M1 审计开始前**定死）。③**§3 复权·费用口径（不 fork）**：唯一权威 = `LEDGER_CONTRACT` + `costs.py`，逐字段冻结 **T+1（`execution_lag_default=1`）/ 单边换手 = 证券腿绝对值之和（现金腿不计费）/ 期初全现金 / 末日 mark-to-market 不强制平仓 / 缺行情不得当 0 收益 / 资金守恒逐日为 0**；**三条硬禁令** —— 同一标的**禁止跨源拼接未校准的前复权序列**（腾讯 qfq 与东财 qfq 基准日不同 ⇒ 接缝处虚假跳变会变成假收益，必须整段单源重取或锚点日校准并把因子写入 manifest）、**禁止依赖 `open` 构造日内收益**（`rotation_backtest.py` 中 `ret_intraday` 已有因 `open` 失真被弃用的先例）、**禁止原地覆盖 `daily-qfq-v1`**（须声明新 `data_version`）；验收须复用既有实现并以「重算既有窗口读数 == 已接受报告逐位」证明同口径（沿用 H5c 做法）。④**§4 访问顺序 P0–P6**：取数审计 → 冻结分割 → Development（可重复）→ **Validation（每假设仅一次）** → 封存 → **OOS（仅一次）** → O1 前瞻；四条铁律（顺序不可跳 / Val 反复读即沦为第二个 OOS / 同时加载两段须留痕 / 默认按 mask 截断）。⑤**§5 未观察 OOS 保留规则**：`holdout` 封存字段（含 `calendar_sha256` / `loader_mask_sha256` / `opened_at` / `open_count`）+ **物理隔离**（P2/P3 的 loader 按 mask 截断，OOS 段**不进入内存**，断言中间产物不含 OOS 日期）+ **一次性令牌**（用户签发、`open_count` 二次读取 **fail-closed**）+ **读取即封档**（无论成败立即 `ACCEPTED` / `ACCEPTED_FAIL`，失败同样是资产）+ §5.5 六条禁令（含**禁止把 `2020` 段包装为 Frozen OOS**、**禁止看过 OOS 后改阈值重跑**、必须登记含被否证的全部假设）。另含 **§1 三层证据模型执行口径** 与 **§7 违规判定与处置**（致命 / 严重分级：已观察段定阈值、看过 OOS 重跑、未冻结即看收益为**致命**；Val 多读、跨源拼接未校准、只纳入存续标的为**严重**；**失败与违规均须留痕，不得静默删除**）。**追加（v1.1，同日裁决 —— `PENDING_RULING` 已全部闭合）**：**§2.4 五项数值门槛正式冻结** —— 暖机期 **250 个交易日** / 停牌上限 **连续 20 个交易日**（严格大于才排除）/ 流动性下限 **最近 60 个交易日日均成交额 ≥ 3000 万元**（取滚动 60 日窗口的**最差**值，保守口径）/ 最小标的数 **每段 ≥ 15 只** / sector 覆盖 **8 个 cluster 中 ≥ 6 个**；同步新增 **§2.6 段级充分性硬门**（Dev / Val / OOS **逐段**核验，不达标回 P1 重划，**禁止放宽逐标的准入凑数**）、§2.3 X2 改以 250 交易日暖机为界、§6.1 审计字段增补 `avg_turnover_60d`（最差 60 日窗口）与 `avg_turnover_seg`（仅参考不判定）、§7 增两条违规（**凑数放宽准入**、**用门槛重判 V2.0.1**）。**门槛只用于 O2 研究宇宙数据准入，不改变 `gen2-rule-v2.0.1` 的 `ACCEPTED_FAIL` 归档，不构成任何参数调优。** |

| `gen2_m1a_data_source_audit_20260915.md` (+ `.json`) | **M1-A 数据源能力与边界审计（10 只老 ETF）**，`COMPLETED` / **待用户裁决**。生成器 `scripts/ml/audit-m1a-etf-history.py`（`--out` / `--cache` / `--offline`，缓存 `%TEMP%/m1a_em_cache`、文件名 = `sha256(url)[:16].json`）。**范围**：仅 `universe_v1.data_note` 所指 10 只老 ETF（`512010`/`512690`/`159928`/`510880`/`512800`/`512000`/`512400`/`515220`/`159941`/`513500`）；**不拼接腾讯序列、不覆盖现有数据、不计算任何策略表现**（未跑回测、未加载规则实现、未改参数）。**五项验证**：① 东财（`push2his`，`klt=101`，`fqt=1/0` **双取**）全历史前复权**完全可得**，10 只行数 **1590–4779**，**首日逐位等于 `etf_master.csv` 真实上市日（10/10，强交叉验证）** ⇒ 决定性证伪「这 10 只只有 2.5 年历史」——那是腾讯 `fqkline` **抓取长度截断**、不是上市日限制；② **复权基准 = 序列最后一个交易日（动态漂移）** ⇒ 同一 URL 不同抓取日返回不同字节、payload sha256 **只在 `generated_on` 当日有效**、跨抓取日/跨源拼接必生假跳变；③ 缺失/停牌：`512010` 缺 2 日、`512690`/`513500` 各缺 1 日，**最长连续缺口均为 1 日 ⇒ 10/10 通过 M4**；④ **成交额 `f57` 齐备（10/10）**，补齐既有腾讯池 `amount` 整列为空的缺口；⑤ **暖机 250 日 10/10 可满足**（`earliest_dev_start_250` = `2008-01-28`～`2021-03-11`）。**新增决定性判据「复权变换类型」**（同源 `fqt=1` vs `fqt=0`：`qfq=raw×k` 且 `k` 分段常数 = **A 比例式**；`raw−qfq` 分段常数 = **B 减法式**；两者皆不成立 = **C 未定**）：**A 6 只**（`512010`/`159928`/`512800`/`512000`/`159941`/`513500`，变化点 **1–18**、失真仅 **1 天** 即份额折算日 —— 该日若用 `fqt=0` 会出现 **−49%～−75%** 的假收益）/ **B 2 只**（`510880` **19** 个变化点且 **41 天负前复权价、最低 `−0.279`**；`512400` **3** 个变化点，非事件日收益被放大 `P/(P−C)` 倍）/ **C 2 只**（`512690` 435/548、`515220` 912/620）—— **与三分类 100% 一致**，把「有多少天异常」升级为「异常为什么发生」。**三分类**：**可纳入 6 / 需人工裁决 4 / 排除 0**（4 只仅在**复权口径**上有疑，可得性/缺口/停牌/暖机/成交额**全部正常** ⇒ 属**方法论裁决**而非数据缺失）。**§2.6 段级充分性（本包最关键结论）**：「每段 ≥ 15 只」**结构性不可达** —— O2 成员须「此前未观察」，而 31 只池（含 5 只 incumbent）**全部已在 B1/B3 被观察** ⇒ 上限就是这 10 只；严格读法（暖机起点 ≤ `2018-04-02`）仅 **6 只、cluster 5/8**，仅取「可纳入」6 只时 cluster 只 **4** ⇒ 按 §2.6 **须扩池或降层（停在 R 层）**，**禁止放宽逐标的准入凑数**。**其他**：与既有腾讯池在 `2024-01-15→2026-09-04`（640 日）**9/10 逐位相同（+0.00pp）**，**仅 `512400` 偏离 +10.63pp**（价格比 1.0318–1.1151 非恒定，东财 +119.73% vs 腾讯 +109.10%）⇒ freeze §3.2 禁令①（禁止跨源拼接未校准前复权序列）的**实证**（**本次未拼接，仅只读比对并记录**）；**生存偏差专项（freeze §2.5 / X6）未量化并已强制声明** ⇒ 该段结论**只能机制否证、不得资格判定**；截断普查：`etf_master` **31 只中截断 11 只**（`data_note` **漏记 `518880`** 黄金ETF）。**失效影响判定：未触碰 ①锁定组件 ②工具链 ③输入 ④已承诺输出 ⇒ B1（`72d5fcd`）与 B3（PR #33 / #34）取证均不失效、无需重跑**（只新增脚本 + 报告 + 本索引行）。**待裁决 6 项（§11）**：**A** 变换机制未定 2 只是否纳入｜**B** 减法式 2 只是否接受、或用分红/折算公告自建比例式因子链｜**C** 是否放行 **O2 扩池**（≥5 只更早上市、未观察、可交易 ETF）｜**D** cluster 基数口径（`candidate_map` 7 / `eligible_codes` 10 / `etf_master` 11，**均 ≠ 冻结文本的 8**；因判据是绝对计数「≥6」故不影响当前结论，但须在 P1 前闭合）｜**E** 生存偏差清单取自哪条数据源（建议列 M1-B 子任务）｜**F** 是否入库（须声明新 `data_version`，候选 `daily-qfq-v2`；建议先裁决 A/B）。**不改变 Gen-2 状态**：仍 `ACCEPTED_FAIL` / `NOT_PRODUCTION_ELIGIBLE`、Shadow / CANARY，**不部署、不提 authority、不写正式仓位**；**未进入新规则实现、未开始 Dev/Val 回测、未完成未观察 OOS 物理隔离**（硬门禁保持）。 |

| `gen2_m1b_o2_expansion_20260915.md` (+ `gen2_m1b_o2_candidates_20260915.json`、`ml/gen2/universe/cluster_taxonomy_v1.json`) | **M1-B：O2 扩池候选清单 · `cluster_taxonomy_v1` 冻结 · 生存偏差证据源**（`FROZEN`）。生成器 `scripts/ml/screen-o2-candidates.py`（`--enum` / `--cache` / `--offline`）与 `scripts/ml/build-cluster-taxonomy.py`。**§2 裁决 D（分类学）**：冻结文本里的「**现有 8 个 cluster**」**全仓库无任何文件枚举过成员**（只在 freeze §2.4/§2.6 与 taskbook §0.9 各出现一次），且 Rule V2 的 bundle **根本不定义 cluster 成员**（只定义 `tech_clusters` 与 `max_core_per_cluster`）。实测四套口径互不相同：`clusters_v1.json` **11** / `etf_master.correlation_cluster` **11** / 30 只 `eligible_codes` 实覆盖 **10** / `candidate_map`（仅 16 只新增）**7**。本包冻结**唯一** `cluster_taxonomy_v1`（L1 sector 10 值 / L2 cluster 11 值 / 逐标的映射），把「8」构造成**可复算**：11 − 3 个非行业 cluster（`broad_beta` 基准、`growth_broad` 宽基成长、`gold_commodity` 商品）= **8**；独立校验路径 `etf_master.sector` 10 值去掉 `broad`/`commodity` = 亦 **8**。一致性：`clusters_v1` vs `etf_master` **0 分歧**、`keyword_rule_v1` 对既有 31 只池 **0 不一致**。**门槛数值 ≥6 未改**，只补分母定义。⚠ **治理缺口**：`clusters_v1.json` 与 `universe_v1.json` **都不在** Rule V2 `immutable_set`（8 项）内 ⇒ 可在 bundle 字节不变时改 cluster 成员，绕过 `max_cluster_weight=0.40` 与 `max_tech_weight=0.65`（与已修补的 `role_thresholds.py`/`regime.py` 同类旁路）。**§3 裁决 C（扩池）**：东财 `clist` **机械枚举 1604 只**（`f26` 上市日覆盖 1604/1604，已用 `159582` 与 M1-A 十只逐位交叉验证）→ 未观察且上市 ≤`2017-12-31` 且非货基/债 = **92 只候选**（≥12 要求 → **缓冲 7.7×**；**全程不读** `f2/f3/f4/f15..f18` 涨跌字段）。**最关键新结论**：O2 的 sector 覆盖**天花板 = 7/8** —— `software_ai` **结构性不可得**（2018 年前市场不存在 AI/软件主题 ETF）；且「今日成交额 ≥3000 万」**代理**筛后行业类仅剩 18 只、覆盖掉到 **5/8**（`tech_hardware` 与 `defensive_dividend` **双双归零**）⇒ **`sector 覆盖` 才是真瓶颈**，「≥12 只+缓冲」真正的作用是**保住 cluster 覆盖**而非数量。**附加**：O2 的 Dev/Val/**Frozen OOS 三段必须全落在 `≤ 2018-04-02`**（观察段自 `2018-04-03` 起）⇒ O2 的 OOS **早于** B3、属**不同制度**，外推限制须在 P1 声明。**§3.6 未完成项**：逐只 `fqt=1` 日线（暖机/停牌/60 日最差成交额）**PENDING** —— `push2his`/`push2` 的 `/api/` 路径对本机返回 **HTTP 000**（IP 限流；`quote.eastmoney.com`/`baidu.com` 正常；备源网易 `chddata` 已 **502** 下线、腾讯 `fqkline` 无成交额）⇒ **候选池与类别覆盖成立，逐标的准入判定未完成，P1 不得据此冻结分割**。**§4 裁决 E（生存偏差）**：主证据 = 上交所 `etf.sse.com.cn/disclosure/ssenotice/` + `www.sse.com.cn/disclosure/announcement/listing/`、深交所 `fund.szse.cn/dynamic/` + `www.szse.cn/disclosure/notice/`；已实测取到 **6 条**终止上市官方公告（`512280`/`516480`/`516690`/`517280`/`159969`/`159646`）并**逐码核验**：6/6 退市码**不在**东财现行枚举内、2/2 存续对照（`159605`/`159607`）**在**枚举内 ⇒ **生存偏差真实存在**；全量清单**未完成**（两所索引无总数、无类型筛选，越往前可得性越差）⇒ 按 freeze §2.5 **O2 结论只能机制否证、不得资格判定**（**不阻断 O2**，因 R 层本就永远不能支持生产资格）。**§6 失效影响**：只**新增**文件，未触碰①锁定组件（`verify-immutable.js` 23/23）②工具链③输入（未覆盖任何 `data_version`）④已承诺输出 ⇒ **B1 `72d5fcd` 与 B3 #33/#34 取证均不失效、无需重跑**；**未进入新规则实现、未开始 Dev/Val/OOS 回测、未完成未观察 OOS 物理隔离**。**§7 数据版本（裁决 F）**：拟名 **`o2-eastmoney-qfq-v1`**（**不用**会被误读为替代线上数据的 `daily-qfq-v2`），须附 payload 哈希/抓取日/复权定义（基准日 = 序列最后一个交易日、**随抓取日漂移**）/标的清单；**禁止**覆盖 `daily-qfq-v1`、**禁止**与腾讯序列拼接。**§8 待裁决 G–L**：G 分母取 8/9/10/11 与 `growth_broad` 归属；H 是否把 `clusters_v1.json` 纳入 `immutable_set`（＝新 lock revision）；I 是否启用「同 cluster 内按 underlying 去重」；J 生存偏差全量抽取立 **M1-C** 还是并入 P1；K 限流解除后是否立即补跑流动性读数；L 是否接受 O2 的 OOS 早于 B3 的外推限制。**⚠ §3.6 勘误指针（2026-09-16）**：该节「`push2his`/`push2` 对本机返回 **HTTP 000**（IP 限流）」的归因**不可复核** —— 已入库 JSON 中 92/92 条 `data_error` 实为 `FileNotFoundError: offline 且无缓存`（`--offline` + 空缓存重跑 ⇒ 该件证明的是**从未发出请求**），**机器可读件中无任何 HTTP 层留痕**（无状态码 / 无 `URLError` / 无重试计数）；且枚举 payload 落于 `%TEMP%\m1b\cache\` 已丢失 ⇒ 92 只候选**不可独立重枚举复现**。⇒ 该节措辞**须按「未复现 / 未留痕」处理，不得再作既证事实引用**。详见 [`gen2_m1b_gap_audit_20260916.md`](gen2_m1b_gap_audit_20260916.md) §1 / §2.4。**本行所指报告正文一字未改**（`FROZEN`），修正**仅经本指针**实现 |

| `gen2_m1b_gap_audit_20260916.md` (+ `.json`) | **M1-B 证据链与口径缺口审计（GAP AUDIT）**，`COMPLETED` / `DOCS_ONLY`（**文档审计包**，2026-09-16 入库；基线 `origin/master` = `6793d7f`，分支 `docs/gen2-m1b-gap-audit`）。**纯只读审计：不改任何代码 / 数据 / bundle / lock / immutable_set / 参数 / OOS 分割。** **八项缺口**：① 现有 92 条 JSON 的 `data_error` 实为 `FileNotFoundError: offline 且无缓存`（`--offline` + 空缓存重跑）⇒ **证明的是从未发出请求，不构成东财限流证据**，报告 §3.6 的 HTTP 000 归因**无机器可读留痕、不可复核**（§1）；② 枚举 payload 落于 `%TEMP%\m1b\cache\` 已丢失 ⇒ 92 只**不可独立重枚举复现**，正式定性 **`AUDIT_SCOPE_SNAPSHOT`（待审计范围快照）**，只可用于「界定要审计哪些只」（§2.4）；③ `_get(tries=8)` × 5 主机轮换会把整轮请求从 110 放大到最多 **880 次**（与被禁的循环重试同构），联网前必须改 **fail-fast + 稳定缓存（移出 `%TEMP%`/`~/.cache`）+ 进度追加留痕** —— 文件级方案 §3.4 / 测试方案 T1–T9 §3.5，均 **`NOT_APPROVED` / 未实施**；④ `92 只` 与 `7(或 5)/8` 均为**候选池并集**，**不是** freeze §2.6 的**段级逐日最小值** ⇒ 两者**无任何逻辑蕴含，既不能证成也不能证伪 §2.6**（§4）；⑤ 生存偏差全量清单未完成，且 `screen-o2-candidates.py` docstring 存在**悬空引用** `gen2_m1b_survivorship_20260915.md`（该文件不存在）；⑥ cluster 分母 8/9/10/11 与 `growth_broad` 归属未闭合；⑦ `clusters_v1.json` / `universe_v1.json` **不在** `immutable_set`（8 项）内 ⇒ 与 `role_thresholds`/`regime` 同类的**旁路治理缺口**；⑧ `software_ai` 结构性不可得 ⇒ O2 sector 覆盖**天花板 7/8**。**Stage A（逐只数据质量取证，44 次请求 / 硬上限 45）= `NOT_APPROVED`，未发任何请求、未重跑枚举**；**G / I 暂不裁决**，且**在 G / I 裁决前，任何候选数与 cluster 数都不得宣称满足 §2.6**（§8）。**冻结门禁全绿**（§9）：`verify-immutable.js` **23/23** · `verify-gen2-build-artifacts.js` **7/7** · `tests/gen2-rule-freeze.test.js` **37 通过/0 失败** · `ml/gen2/tests/test_wp_g2_04_freeze.py` **18 tests OK**。另澄清项目哈希口径为 **UTF-8 + CRLF→LF 归一化后 sha256**（非原始字节），引用 `bundle_sha256` / `lock_sha256` 时必须声明（§9.3） |

| `gen2_m2_failure_attribution_20260915.md` (+ `.json`) | **M2：Rule V2.0.1 经济失败的只读后验归因**，协议 **`POSTMORTEM_NOT_FOR_CALIBRATION`**（用户裁决 2026-09-15）。生成器 `scripts/ml/attribute-gen2-v2-failure.py`（`--out` / `--tmp`）。**方法**：**加性（telescoping）五层分解**，每层一次账本重算、零参数改动：`A main5_equal_weight` →（选池范围）→ `B universe_equal_weight` →（角色选池）→ `C core_equal_weight`〔**诊断合成锚点**，每日 CORE 等权、无上限，**不进 bundle / 不作为候选参数**〕→（权重·上限）→ `D gen2_v2_undefended` →（防守）→ `E gen2_v2_defended` →（换手·成本）→ `F E@10bps`。**三重身份证明**：① 磁盘重算 `bundle_sha256 = fabd31d9…` == B1(`ACCEPTED`)/B3(`ACCEPTED_FAIL`) manifest 声明值，`lock_revision = 3`、`lock_sha256 = d3d40f99…`、`immutable_set` 8 项、`config_drift = []`；② B1 完整复算逐字段比对 ⇒ **`all_match = True` / `mismatched_cells = 0`**；③ OOS 经济读数与 **B3 报告 §6 表格逐位相同**。**核心结论（OOS `2021-01-04 → 2026-09-04`，1376 日）**：`A→B` **选池范围 −220.58pp**（**压倒性主因**）· `B→C` 角色选池 **+15.44** · `C→D` 权重·上限 **−6.82** · `D→E` 防守 **+17.94** · `E→F` 换手·成本 **−19.62** · **`A→F` 总差 −213.64pp**（加性严格闭合）。⇒ **失败主因不在规则层**：把基准从 Main5 换成自家 30 只全池，规则层 `@0bps` **净增 +26.56pp**、**`@10bps` 主判据档净增 +9.00pp**（`universe_equal_weight` +28.54% → `gen2_v2_defended` +37.54%）。同窗口 **`market_510300` 仅 −3.75%** 而 `main5_equal_weight` **+251.18%** ⇒ C1 基准本身是「OOS 期极度集中的赢家篮」。**机制层最关键新事实**：`rule_v2_ab` 先全体 `target_weight = 0.0`、**只有 `role == "CORE"` 拿到非零权重**（`CHALLENGER`/`SATELLITE` 恒 0）⇒ **`CORE == 0` 的交易日整仓现金**：**全候选面板 731/2046 = 35.73%**、**OOS 396/1376 = 28.78%**；`target_weight = min(1/|CORE|, 0.25)` ⇒ `|CORE| ∈ {1,2,3,4}` 必然被单只上限截断。绑定读数（OOS 980 个有 CORE 日）：单只 `0.25` **612 日（62.4%）**、cluster `0.40` **647 日**、科技 `0.65` **仅 137 日（10.0%）** ⇒ 真正主导的约束是单只与 cluster，**不是**科技上限。⚠ **本包修正了一处自身诊断缺陷**：首版误把候选面板 **`CASH` 腿**（权重 = 1 − Σ证券，均值≈0.50 ≥ 0.25）纳入统计，假触发单只上限；已按 `portfolio_builder._assert_caps` 真实口径（**该函数在追加现金腿之前调用**；单只看全部**证券行**，cluster/科技**仅看 `CORE` 行**）重写，并以「证券行残差现金 `0.4992` == 账本 `avg_cash_weight 0.4991`」四位一致自证。**逐年（0bps）**：C4 无灾难年成立（`defended` 六年最差 2023 `−5.80%`）；**2023 最痛**（`main5 +42.11%` vs `universe −4.77%`，单年选池差 ≈47pp）；防守层正贡献**集中在 2022/2025/2026**（2021/2023/2024 为负）⇒ regime 依赖。MDD `@0bps`：`defended −21.50%` 优于 `main5 −34.57%`、`universe −49.72%`（**B3 的 −23.35% 是 `@10bps` 口径，非分歧**）。**新假设 H1–H5（只出假设+可证伪判据，不产参数）**：H1 基准选择偏差｜**H2 `CORE==0` 空仓是主要拖累**｜H3 cluster 上限在科技主导期为负贡献｜H4 成本主要来自角色进出换手｜H5 防守不可外推；验证**只能**落 O2 未观察段（`≤ 2018-04-02`）或 O1 前瞻，**严禁**在 B3 OOS 上验证。**§8 失效影响**：只**新增**文件，未触碰①锁定组件（`verify-immutable.js` **23/23**、`freeze-…--check` **PASS**）②工具链③输入④已承诺输出 ⇒ **B1/B3 取证均不失效、无需重跑**；`scripts/test-all.js` = **45/45 通过**。**§9 待裁决 M1–M4**：M1 是否批准 H2 独立诊断重算｜M2 H1/H3 暂缓至 M1-B（§3.6 流动性仍被东财限流阻断）还是先做 H4｜M3 是否把 `C→D` 拆成「截断+残差现金」与「cluster/科技缩权」两子项｜M4 是否入库。**明列不做**：无参数搜索/网格/阈值调整、无新 bundle/lock、未为找通过结果重跑 OOS、未改 `ACCEPTED_FAIL` 归档状态、未部署/未提 authority/未写正式仓位、未进入新规则实现、未开始 Dev/Val/OOS 回测 ⚠ **§6 H3 机制解释勘误**（数值不变）：见 `gen2_m2_h3_erratum_20260916.md`（本行 ↔ 勘误行 双向链接） |

| `gen2_m2b_targeted_attribution_20260916.md` (+ `.json`) | **M2-B：定向归因**（用户裁决 2026-09-16 批准的三项），协议 **`POSTMORTEM_NOT_FOR_CALIBRATION`**。生成器 `scripts/ml/attribute-gen2-v2-failure-targeted.py`（`--out` / `--tmp`，约 4m20s）。**【T1】`C→D1→D2` 会计式拆分**（严格照冻结实现 `rule_v2_ab` 第 329–365 行的执行顺序）：`C→D1` **单只截断** `min(1/|CORE|, 0.25)` 及其新增残差现金 **−14.83pp** ｜ `D1→D2` **cluster(0.40)/科技(0.65) 缩权**及其新增残差现金 **+8.02pp** ｜ 加总 **−6.82pp == 原 `C→D`**（`exact_identity = true`、`identity_gap_pct = 0.0`；残差**全部留作现金**、**未**重分配给其他 ETF）。⇒ **直接修正 M2 的 H3 机制前提**：`C→D` 的负贡献来自**单只截断**、**不是** cluster 上限 —— cluster/科技缩权在 OOS 是**正贡献**；`D1→D2` 内部成因 cluster 缩权 Σw `−51.10`（**90.7%**）vs 科技 `−5.25`（9.3%）。残差现金（OOS）：`C 0.2878` → `D1 0.4618` → `D2 0.4992`，即单只截断**多制造 +17.41pp 现金**却损失 −14.83pp。**【T2】H2 纯描述性诊断**（**未构造任何替代规则、未跑替代账本**）：`CORE==0` OOS **396/1376 = 28.78%**（全窗 731/2046 = 35.73%），OOS 15 段、最长 **91 连续交易日**、均值 26.4 日；分年 2022 最重（137 日）；`CORE==0` 日市场 `510300` 累计 **−37.55%**、前瞻 20 日均值 +0.62%（中位 −0.55%）⇒ **「错失收益」在本段内不显著**。**状态机漏斗（本包最关键读数）**：396 日**每天都有** alpha 够格标的（`alpha_pct ≥ core_pct = 0.80`，日均 **4.27 只**，`days_with_zero_alpha_qualified = 0`），叠加 `trend_gate` 后日均 1.79 只（但 146 日为 0）—— 而 CORE 仍是 0；`reason_codes` 绝对主因 **`PROMOTION_BLOCKED_BY_PERMISSION` 1498**（次为 `PROMOTION_HYSTERESIS` 67 / `NO_CORE_TREND_GATE` 18）；`selection_mode` = **`DISABLED` 375** / `ACTIVE` 11 / `REDUCED` 10；交叉表 RISK_OFF 636 日中 375 日 `CORE==0`（**58.96%**）vs 非 RISK_OFF 仅 21 日（**2.84%**）⇒ **`CORE==0` 不是「选不出」，而是：RISK_OFF 期间禁止新晋升（不强制清除既有 CORE）；当既有 CORE 因其他状态转换已归零后，系统没有回补路径，因此可形成持续 `CORE==0` 的现金状态**，只能等 `market_score ≥ 55` 才解冻（`selection_permission.selection_mode = DISABLED`）。**【T3】H4 换手归因**（只归因，未改费用档/频率）：换手来源 `entry` 33.51% / `exit` 28.34% / **`reweight` 38.15%**（成本分摊 4.47 / 3.78 / **5.09**%，合计 13.34% 闭合）；二次切分**角色切换驱动 55.30%** vs **非切换 44.70%**（M2 §6 的 H4 判据「>60% 非切换 ⇒ 否证」**未被触发**，但差距仅 10.6pp ⇒ **不足以支撑「成本主要来自角色进出」的强表述**）；调仓频率 **1116/1376 = 81.1% 日有换手、中位间隔 1 天**、最长间隔 91 天；成本拖累 **−19.62pp**（`0bps +57.16%` → `10bps +37.54%`）；OOS 平均现金 **0.5671**、`cash ≥ 90%` **282 日**；防守 `RISK_OFF` **636 日（46.22%）**、波动目标缩仓 940 日（最小 `0.263`）、hedge 非零 636 日、防守前后敞口 0.5009 → 0.4329。**四重身份证明**：① B1 复算 `all_match = True` / `mismatched_cells = 0`｜② **D2 权重复现** `max_abs_diff = 0.000e+00`（3,703 行 CORE，证明拆分与冻结实现**逐位同源**）｜③ 换手复现器 vs 权威账本 `max_abs_turnover_gap = 1.11e-16`｜④ 与 M2 的 9 项读数**逐位一致**。**未触碰**：规则 / 参数 / 数据版本 / lock / `immutable_set`（仍 **8 项**、`lock_revision = 3`，**未**纳入 `cluster_taxonomy_v1`）/ OOS 分割；**未验证 H1/H3**（按裁决暂缓）；报告 §8 的 **G–L 六项仍 `PENDING_RULING`**。**新增待裁决**：**N1** 是否为 M2 报告 §6 的 H3 行单立勘误 PR｜**N2** 是否入库本包｜**N3** 是否把「RISK_OFF 期间 CORE 归零后无法回补」立为独立机制假设 **H6** |

| `gen2_m2_h3_erratum_20260916.md` | **M2 §6 H3 机制解释勘误**（`ERRATUM`；用户裁决 2026-09-16 **N1 单立**）。**原报告 `gen2_m2_failure_attribution_20260915.md` 一字未改**。① **数值一律不变**：`C→D = −6.82pp`，连同 `A→B` −220.58 / `B→C` +15.44 / `D→E` +17.94 / `E→F` −19.62 / `A→F` −213.64（0 bps）全部照旧。② **机制解释错误**：原把 `C→D` 的负贡献**主要归因于 cluster 上限**，方向相反（口径原因是 M2 只做单层差分，无法区分两个方向相反的子效应）。③ **正确拆分**（依据 M2-B §3，`exact_identity = true` / `identity_gap_pct = 0.0`）：`C→D1` **单只截断** `min(1/|CORE|, 0.25)` = **−14.83pp**｜`D1→D2` **cluster(0.40)/科技(0.65) 缩权** = **+8.02pp**｜加总恒等 = **−6.82pp**（残差**全留现金**、未再分配；`D1→D2` 内部 cluster 缩权占 **90.7%** / 科技 9.3%）⇒ **负贡献来自单只截断，cluster/科技缩权在 OOS 是正贡献**。**不改**任何参数/阈值/lock（`immutable_set` 仍 8 项、`lock_revision = 3`、未纳入 `cluster_taxonomy_v1`）、**不验证** H3（仍暂缓）、不改 Rule V2.0.1 归档（`ACCEPTED_FAIL` / `NOT_PRODUCTION_ELIGIBLE`）。互链：M2 行 ↔ 本行；相关机制假设见 `gen2_h6_risk_off_zero_core_20260916.md` |

## 旧角色语义审计基线（**不可逐位比较**）

| 报告 | 说明 |
|---|---|
| `gen2_rule_baseline_report.md` | 旧角色语义 / 旧账本口径，仅作审计基线 |
| `gen2_rule_baseline_universe_v1_30.md` | 旧角色语义 / 旧账本口径，仅作审计基线 |
| `gen2_qualification_2026-09-08.md` | 旧角色语义 / 旧账本口径，仅作审计基线 |
| `oos_walk_forward_2026-09-07.md` | 旧角色语义 / 旧账本口径，仅作审计基线 |
| `gen2_v21_m2_validation_2026-09-09.md` | 旧角色语义 / 旧账本口径，仅作审计基线 |
| `gen2_v21_m3_validation_2026-09-09.md` | 旧角色语义 / 旧账本口径，仅作审计基线 |
| `gen2_v2_archive_2026-09-09.md` | 旧角色语义 / 旧账本口径，仅作审计基线 |
| `gen2_p1_rule_v2_ab.md` | 旧角色语义 / 旧账本口径，仅作审计基线 |

## 非经济类（不受角色语义影响，保持有效）

| 报告 | 内容 |
|---|---|
| `gen2_p0_batchA_fixes.md` / `gen2_p0_batchB_fixes.md` / `gen2_p0_experiment_framework_fixes.md` | P0 修复记录 |
| `universe_data_quality_v0.md` | 数据质量报告 |

## Gen-2 机制假设登记（**仅假设，未验证**）

| 报告 | 内容 |
|---|---|
| `gen2_h6_risk_off_zero_core_20260916.md` | **`H6_RISK_OFF_ZERO_CORE_ABSORPTION`**（用户裁决 2026-09-16 **N3** 立档；`HYPOTHESIS_REGISTERED` / **未验证**）。**假设**：RISK_OFF 的**晋升禁令**与**既有 CORE 耗尽**共同作用，可能造成持续零 CORE / 高现金暴露。**机制链条**（读源码 + 读 M 层读数）：`RISK_OFF → selection_mode = DISABLED`（**禁新晋升、不清现任 CORE**；`max_core_count` 返回 `None`）→ 既有 CORE 因 **`NO_CORE` 趋势硬门槛**（`px_ma60 > 0` 不成立 ⇒ `NO_CORE_TREND_GATE`）或 **demotion 滞后**（`≥ 5` 日）归零 → 归零后**无回补路径**（`PROMOTION_BLOCKED_BY_PERMISSION`）→ `rule_v2_ab` 下只有 `CORE` 拿非零权重 ⇒ **整仓现金**，持续到 `market_score ≥ 55` 解冻。**M 层读数（证据力 0）**：OOS `CORE==0` **396/1376 = 28.78%**、最长 **91 连续日**；RISK_OFF 636 日中 **375（58.96%）** vs 非 RISK_OFF 仅 2.84%；`PROMOTION_BLOCKED_BY_PERMISSION` 主因 **1,498**；漏斗 **`days_with_zero_alpha_qualified = 0`**（396 天每天都有 alpha 够格标的、日均 4.27 只）⇒ **不是「选不出」**。**验证路径唯一**：O2 **未观察段**（三段全落 `≤ 2018-04-02`，且须待 M1-B 逐只流动性审计完成 + P1 分割冻结之后）或 **O1 前瞻段**；**数值阈值不在本档预设**，须于 P1 pre-registration 与 H2 判据一并冻结（纪律：不自创阈值）。**禁令**：不设计替代规则、不在已观察 B3 OOS 测试替代行为、不产参数/阈值/lock、不改归档状态（`ACCEPTED_FAIL` / `NOT_PRODUCTION_ELIGIBLE`）。与 **H2** 为**机制细化关系**，并存不替代。**互链**：`gen2_m2b_targeted_attribution_20260916.md` §4/§4.5（证据来源）｜`gen2_m2_h3_erratum_20260916.md`（H3 勘误） |

## WP-G2-08 治理草案（HUMAN_RULINGS_RECORDED · CONTRACTS_NOT_APPROVED · NOT_IN_FORCE · NON_AUTHORITATIVE）

| 草案 | 内容 |
|---|---|
| `gen2_source_contract_draft_20260917.md` / `gen2_collection_reliability_contract_draft_20260917.md` / `gen2_source_evaluation_matrix_draft_20260917.md` | **WP-G2-08 治理草案（3 份治理草案；`HUMAN_RULINGS_RECORDED`；`CONTRACTS_NOT_APPROVED`；`NOT_IN_FORCE`；`NON_AUTHORITATIVE`。）**：① **来源资格契约草案** —— `EC-01`–`EC-08`（单源整段可得 / 复权可判定 / 基准日可追溯 / 日历 / 字段完备 / 覆盖 / 许可与再分发 / 可复现性），数值一律**引用** freeze v1.1；② **取数可靠性契约草案** —— 请求预算 `B-*` / 稳定缓存 `C-*` / 哈希 `H-*` / 进度与失败 `P-*`，外加**「准入模式 vs 诊断采集模式」的严格边界**（**`V-01` 暂不立项**；诊断模式**未获实现、联网或取数授权**，仅保留候选框架；**`V-02`～`V-05` 为未生效 / 不新增机制**——`V-02` 数值当前不得作为现行规则且不定执行预算、`V-03` 为条件式、`V-04` **不新增**独立拒绝事件台账、`V-05` **不新增**显式联网确认开关，均不得影响现有准入门禁）；③ **来源评价矩阵草案** —— **未填来源评分；权重和 TTL 保持空白，部分治理边界已裁定**（`W-01`/`W-02` **暂不定值**、`W-03` **不允许降级使用**、`W-04` **三类事件触发且无固定 TTL**、`W-05 = PENDING`），只定义维度与硬门规则，**不含任何具体供应商**。三份**均 `NOT_APPROVED` / 未生效**（⚠ **「裁决已记录」≠「契约已批准」**：具体 U/V/W 项已由 Human 裁定，**但三份文档本身仍未整体批准**），不改任何冻结件、不改变任何既有归档状态。**`U/V/W` 共 16 项状态已回填，其中 `OPEN` / `PENDING` 项不构成生效参数**（`U-01`–`U-06` / `V-01`–`V-05` / `W-01`–`W-05`）；其中 `U-04` = **覆盖率分母不扣除**；冻结的 **`≥ 6 of 8`** 分母**不得因 `STRUCTURAL_NO_INSTRUMENT` 或任何结构性不可得方向而缩减**；**`STRUCTURAL_NO_INSTRUMENT` 仅作事实记录，不得用于准入结论**（附 **`freeze v1.1` §2.6 Scope Note**：该口径**不改** freeze 的 8-cluster 研究宇宙定义；改 cluster denominator 须另立 `freeze-change package`）。`V-01` = **暂不立项**（不构成诊断模式实现、联网取数或 `WP-G2-09` 的授权）。⚠ 本轮另记录一起**历史并发版本冲突记录**：三份契约草案的裁决章曾被改写为 agent 自补默认值并标为 `LOCAL_OWNER_RULING_ADOPTED`（**不具有 Human / Owner 裁决效力，已从正文移除**），现场快照与取证见仓库外 `C:/Users/iquel/gen2-o2-evidence/wp-g2-08-20260917/concurrent-write-snapshot/`（**证据只保存，不进正式契约正文**） ｜ **`U-04` 勘误（双向索引）** → `gen2_source_contract_u04_erratum_20260917.md`（**文档勘误；仅在该文件与被勘误契约同时进入 `master` 后生效；不改变 `freeze`、规则或生产行为。**） |
| `gen2_source_contract_u04_erratum_20260917.md` | **`U-04` 勘误**（`STATUS: DOCUMENTARY_ERRATUM` · 2026-09-17；2026-09-18 措辞收口）：被勘误对象 = **本工作区当前** source contract 的 `U-04`（对照：入库版本 `3b8047e0`）。现行唯一有效裁决 = **覆盖率分母不扣除；冻结的 `≥ 6 of 8` 分母不得因 `STRUCTURAL_NO_INSTRUMENT` 或任何结构性不可得方向而缩减；`STRUCTURAL_NO_INSTRUMENT` 仅作事实记录，不得用于准入结论。**`3b8047e0` 的「从分母中扣除」表述为**被勘误旧表述**，同样**不得用于准入结论**。**不改** freeze v1.1 的 `≥ 6 of 8` 分母、**不改**代码 / 规则 / 生产行为；不讨论 `U-01` / `U-02` / `U-03` / `U-05` / `U-06`。**生效条件：仅在该文件与被勘误契约同时进入 `master` 后生效；不改变 `freeze`、规则或生产行为。** 互链：`gen2_source_contract_draft_20260917.md`（文首 / `EC-06` / §9 表 / §9.1 / §9.2 / §10 / §11 **双向链接**） |
