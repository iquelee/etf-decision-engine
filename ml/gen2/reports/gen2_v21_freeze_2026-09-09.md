# Gen-2.1 v2.1.0 Freeze 归档记录（2026-09-09）

状态：**M3 APPROVED → Freeze Point 执行完毕**。`gen2-rule-v2.1.0` 已冻结（Gate-OFF 配置），SHA immutable。

## 一、冻结产物

| 文件 | 内容 |
|---|---|
| `ml/gen2/manifests/GEN2_RULE_V21_BUNDLE.json` | FROZEN 运行参数真相（engine_id=gen2-rule-v2.1, bundle_version=gen2-rule-v2.1.0, state=FROZEN）。含 alpha/regime/selection/portfolio/defense/production（V2 基线）+ cluster_leadership（retained 固化）+ consolidation_gate（**enabled=false, min_quality=null**）+ turnover_aware_replacement（5 硬门 + design-fixed 常数）+ protocol |
| `ml/gen2/manifests/GEN2_RULE_V21_LOCK.json` | bundle_sha256=`eb53441e…`、sealed_at=2026-09-09 |
| `scripts/verify-immutable.js` | 扩至 **14 项**：GEN1×4 + V361×2 + GEN2_V2×2 + **GEN2_V21×2** + root-of-trust×4（含 V21 LOCK anchor `ecff201d…`） |
| `ml/gen2/manifests/GEN2_RULE_V21_DRAFT.json` | 归档 `state=SUPERSEDED_BY_V210_FROZEN`（历史研究记录，非运行时真相） |

## 二、v2.1.0 冻结配置要点

```text
Consolidation Gate    enabled=false, min_quality=null（M3 2024 三臂: Gate ON 无成本后增量）
Cluster Leadership    enabled=true（top4/min1/breadth0.5 validation-retained; leaders2 design-fixed）
Replacement 硬门      5 hard gates（min_hold 5 / cost_bps 10 / alpha_to_excess 5 / breakeven 20 / buffer 5，全 design-fixed）
weight_delta 规则     projected_trade_weight（tentative CORE 集经 25/40/65 cap）
```

## 三、冻结纪律

- **v2.1.0 参数不再允许修改**；改 replacement / cluster / Gate 逻辑须**起新 bundle_version**（v2.1.1 等），不得静默改冻结包。
- 任何改动 bundle+lock 同 commit → root-of-trust 锚 FAIL；合法升级须显式改 verifier ROOT_ANCHORS（PR diff 醒目）。

## 四、评价地位（contaminated 声明）

- M3 早期查看过全样本（含 2025+）于 Freeze 前 → **2025+ 历史段为 contaminated / post-hoc**。
- M4/M5 在 2025+ 的结果只作 **post-hoc historical diagnostics**，不得宣称 Frozen Evaluation。
- **最终资格由 future Live Shadow 积累提供**。

## 五、M3 关键证据摘要（2024 Validation，cost10）

- 三臂成本后均跑输 Main5（+30.5%）：gate_off **+0.7%** / gate_40 −3.1% / gate_60 −1.3%。
- Replacement Payoff 双重读数（gate_off 14 替换）：vs incumbent 20D **−1.81%** / 40D −2.62%（被换现任更强）；vs cluster 20D +0.82% / 40D −0.18%（challenger 仅略强簇均且不持久）→ incumbent 显著强于簇均 ≈+2.6% → **劣质换仓**。
- Gate 收紧机械生效：Turnover 16.7→7.8、MDD −12.3%→−4.4%，但未转化为成本后增量。
- bootstrap vs Main5：gate_off [−0.00280, −0.00001]（显著为负）。
